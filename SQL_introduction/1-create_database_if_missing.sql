-- This SQL command creates a new database named 'hbtn_0c_0' if it does not already exist.
-- If the database already exists, the command will not raise an error.
CREATE DATABASE IF NOT EXISTS hbtn_0c_0;
